package entities.actions;

import entities.Triangle;

public interface BaseTriangleActions {
    public double calculatePerimeter(Triangle triangle);
    public double calculateArea(Triangle triangle);
}
